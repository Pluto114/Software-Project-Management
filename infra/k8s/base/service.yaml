# ============================================================
# Kubernetes Service 定义
# 职责：服务发现与负载均衡
# ============================================================

# TODO: Service: realtime-hub (ClusterIP, 内部 WebSocket 路由)
# TODO: Service: telemetry-service (ClusterIP)
# TODO: Service: control-service (ClusterIP)
# TODO: Service: rule-engine-service (ClusterIP)
# TODO: Service: do-forecast-service (ClusterIP)
# TODO: Service: command-center-web (LoadBalancer, 对外暴露 80/443)
# TODO: Service: redis (ClusterIP, Headless)
# TODO: Service: influxdb (ClusterIP)
# TODO: Service: mysql (ClusterIP)
#
# TODO: Ingress: TLS 终止 + 路由规则
#   /ws/*  -> realtime-hub (WebSocket 支持)
#   /api/* -> 各微服务 API 路由
#   /*     -> command-center-web (SPA)
