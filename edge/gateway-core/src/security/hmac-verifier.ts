/**
 * HMAC 安全验签模块
 *
 * 职责（需求文档 3.4.2 & 4.3.1）：
 * 1. 验证云端下发指令的 HMAC-SHA256 签名
 * 2. 防重放攻击：检查 64位随机 Nonce 与时间戳，有效窗口 2000ms
 * 3. 验签通过后指令才能下发至继电器执行
 * 4. 手动优先原则：现场物理开关拥有最高优先级
 *
 * Safety Contract:
 * - 所有云端下发的控制指令必须包含 timestamp + nonce + HMAC 签名
 * - 边缘网关进行硬件级验签
 * - 手动操作状态实时同步至云端
 */

// TODO: 实现 HMAC-SHA256 签名验证
// TODO: 实现 Nonce 去重缓存（防重放）
// TODO: 实现时间戳窗口校验（2000ms 有效期）
// TODO: 实现手动/自动优先级仲裁逻辑
