/**
 * Modbus-RTU 主站模块
 *
 * 职责：
 * 1. 通过 RS485 总线轮询传感器从站
 * 2. 寄存器映射（根据需求文档 6.1.1）：
 *    - 0x0001: 溶解氧(DO), UINT16, mg/L, 缩放0.01
 *    - 0x0002: 水温(Temp), INT16, ℃, 缩放0.1
 *    - 0x0003: 酸碱度(pH), UINT16, 缩放0.01
 *    - 0x0004: 设备电流, UINT16, A, 缩放0.1 (判断增氧机负载)
 * 3. 容错：CRC-16 校验，连续3次失败标记传感器为"不可信"
 */

// TODO: 实现 Modbus-RTU 帧构建与解析
// TODO: 实现 100ms 定时轮询循环
// TODO: 实现 CRC-16 校验与连续失败计数
// TODO: 实现传感器状态管理（正常/不可信/离线）
